<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.help.back.backend.dao.UserDao">
    <select id="getUsers"  resultType="user">
        select id, email, name, phone_number , birthday from users
    </select>
    <select id="getUsersByName" parameterType="String" resultType="user">
        select id, email, name, phone_number  , birthday from users where name LIKE "%${value}%"
    </select>
    <select id="getUsersByEmail" parameterType="String" resultType="user">
        select id, email, name, phone_number , birthday from users where email LIKE "%${value}%"
    </select>
    <select id="getUsersById" parameterType="int" resultType="user">
        select id, email, name, phone_number , birthday from users where id = #{value}
    </select>
    <insert id="postUser" parameterType="user" useGeneratedKeys="true" keyProperty="id" >
        INSERT INTO
        users(password, name, email, birthday, phone_number)
        Values(SHA2(#{password},256), #{name}, #{email}, #{birthday}, #{phoneNumber})
    </insert>
    <update id="updateUser" parameterType="user">
        UPDATE users SET password = SHA2(#{password},256), name = #{name}, email = #{email}, birthday = #{birthday}, phone_number = #{phoneNumber}
        WHERE id = #{id}
    </update>
    <delete id="deleteUser" parameterType="int">
        DELETE FROM users WHERE id = #{value}
    </delete>
    <select id="login" parameterType="login" resultType="user">
        SELECT id, email, name, phone_number, birthday
        FROM users
        WHERE email = #{email} AND password = SHA2(#{password}, 256)
    </select>
</mapper>
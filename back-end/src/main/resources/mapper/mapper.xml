<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.help.back.backend.dao.UserDao">
    <select id="getUsers" parameterType="com.help.back.backend.domain.User" resultType="com.help.back.backend.domain.User">
        select * from user
        <where>
            <if test="user_id != null">
                user_id LIKE "%${user_id}%" </if>
            <if test="email != null">
                OR email LIKE "%${email}%" </if>
            <if test="name != null">
                OR name LIKE "%${name}%" </if>
        </where>
    </select>
    <insert id="postUser" parameterType="com.help.back.backend.domain.User">
        INSERT INTO
        user(user_id, password, name, email, birthday, phone_number)
        Values(#{user_id}, #{password}, #{name}, #{email}, #{birthday}, #{phone_number})
    </insert>
    <update id="updateUser" parameterType="com.help.back.backend.domain.User">
        UPDATE user SET password = #{password}, name = #{name}, email = #{email}, birthday = #{birthday}, phone_number = #{phone_number}
        WHERE user_id = #{user_id}
    </update>
    <delete id="deleteUser" parameterType="String">
        DELETE FROM user WHERE user_id = #{value}
    </delete>
</mapper>